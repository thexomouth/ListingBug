      case "automations": return isLoggedIn ? <AutomationsManagementPage onViewDetail={handleViewAutomationDetail} initialTab={automationsInitialTab} onNavigate={navigateWithLoading} onViewRunDetails={(run) => { setSelectedAutomationRun(run); sessionStorage.setItem('listingbug_selected_automation_run', JSON.stringify(run)); navigateWithLoading('automation-run-results'); }} /> : <LoginPage onLogin={handleLogin} />;
      case "automation-run-results": return isLoggedIn && (selectedAutomationRun ?? (() => { try { const s = sessionStorage.getItem('listingbug_selected_automation_run'); return s ? JSON.parse(s) : null; } catch { return null; } })()) ? (
        <AutomationRunPage run={selectedAutomationRun ?? (() => { try { const s = sessionStorage.getItem('listingbug_selected_automation_run'); return s ? JSON.parse(s) : null; } catch { return null; } })()} onBack={() => { navigateWithLoading('automations'); }} />
      ) : <LoginPage onLogin={handleLogin} />;