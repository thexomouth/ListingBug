@echo off
cd /d "C:\Users\User\Downloads\ListingBug FIGMA MVP"
git commit -m "fix: batch fixes - stripe checkout v5 edge fn, api key session auth, billing date guard, plan modal 2col + upgrades text, integrations tab cleanup, automation history dark bg, search zero state dark mode, field mappings step removed from wizard"
git push origin main
